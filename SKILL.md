---
name: remember
description: Claude Code 跨会话持久记忆——写直通日志 + 水位线/内容哈希双闸去重 + 开场注入最近存档 + 健康自检。
license: MIT
---

# Remember —— Claude Code 跨会话记忆系统（通用模板 v2）

> 让 Agent 每次开场都记得发生过什么：对话永远不丢、同一段对话永不归档两次、开场加载的永远是最新且不重复的那几场。

---

## 1. When to Read（何时读取）

**SessionStart 钩子会自动加载记忆**（`load-last-chat.ps1` 把最近 5 份存档注入上下文）。不需要手动读旧存档，除非：

- 钩子输出没出现在上下文里（少见，Termux/CI 环境）
- 用户说"不忘""remember""记忆还在吗"
- 需要 5 份之外更早的上下文 → 用 `CHATLOG_INDEX.md` 按日期找

**跳过读取：** 一次性任务、纯调试、或用户说"别读旧存档"。

---

## 2. 目录结构

```
<项目>/.claude/memory/chatlog/
├── _pending.md            # 活缓存——当前会话，尚未归档（追加式）
├── _state.json            # 状态——每个 transcript 的归档水位线 + 缓存归属
├── CHATLOG_INDEX.md       # 全量索引——每次归档后从磁盘重建
├── YYYYMMDD/              # 按日期分目录
│   └── HHmmss_auto.md     # 归档存档——第一行是内容哈希
```

| 约定 | 规则 | 例 |
|------|------|----|
| 日期目录 | `yyyyMMdd`，无分隔符 | `20260825` |
| 归档文件 | `HHmmss_auto.md`，24 小时制 | `163452_auto.md` |
| 编码 | UTF-8（无 BOM） | — |
| 索引路径 | 正斜杠 `/` | `20260825/163452_auto.md` |

---

## 3. 文件格式

### 3.1 `_pending.md` — 活缓存

追加式，永不修改已写内容，只追加：

```markdown
# Auto-save -- YYYY-MM-DD

> Generated by auto-save. Cleared after formal archive.

---

[msg:UUID]
**User** (YYYY-MM-DD HH:mm:ss):
内容

[msg:UUID]
**Assistant <personaName>** (YYYY-MM-DD HH:mm:ss):
内容
```

- 每轮 2 个 `[msg:UUID]` 块（用户 + 助手）
- 归档后**删除**（不是清空）
- 已被水位线标记为"已归档"的消息永远不会再次进入缓存

### 3.2 归档 `YYYYMMDD/HHmmss_auto.md`

```markdown
<!-- h:<40位 sha1 正文哈希> -->
# Session Archive -- YYYY-MM-DD HH:mm

## Conversation

**User**:
内容

**Assistant <personaName>**:
内容
```

- **第一行是正文内容哈希**（只算 `## Conversation` 往后的部分）——同一场对话永远哈希相等
- 全文保留、永不总结、永不改写
- **永久保存，绝不自动删除**

**自检：**"用户一年后回看这份，能不能拿到一字不差的对话？"不能就修格式。

### 3.3 `CHATLOG_INDEX.md` — 全量索引

```markdown
# CHATLOG_INDEX

last5: 20260825/163452_auto.md, 20260825/155010_auto.md, ...

## 20260825

- 16:34 | 163452_auto.md | 一句话摘要
```

- **列出全部存档**（按日期分组，新的在前）——每次归档后从磁盘全量重建，旧存档不会从索引消失
- `last5`：最新 5 条路径，供开场加载钩子用（新的在前）
- 摘要 ≤ 15 字，取自每条存档的第一条用户消息

### 3.4 `_state.json` — 水位线与归属

```json
{
  "pendingTranscript": "C:/path/to/project/xxx.jsonl",
  "archived": {
    "C:/path/to/project/xxx.jsonl": "最后一条已归档消息的UUID"
  }
}
```

- `pendingTranscript`：当前活缓存属于哪场对话
- `archived[transcript]` = 该 transcript 已归档到的最后一条消息 UUID（水位线）
- **凡是 ≤ 水位线的消息，永不再次进入缓存、永不再次归档**

---

## 4. 配置

`remember.config.json` 放在钩子脚本旁边；环境变量可覆盖。

| 字段 | 默认值 | 作用 |
|------|--------|------|
| `personaName` | `Assistant` | 存档里助手消息的署名 |
| `marker` | `【不忘】` | 开场注入记忆块的标记前缀 |
| `chatlogRoot` | 自动推导 | 存档目录绝对路径覆盖（全局安装必填） |

环境变量：`REMEMBER_PERSONA`、`REMEMBER_MARKER`、`REMEMBER_CHATLOG_ROOT`。

---

## 5. 操作

### 5.1 写入 —— 每轮追加（Stop 钩子，无条件）

1. 解析当前 transcript（hook stdin → session_id → 项目内最新；`subagents/` 目录的转录永远不选）
2. 崩溃恢复：若缓存属于别的 transcript，先归档它再继续
3. 跳过所有 ≤ 水位线的消息
4. 追加未见过的新消息；500ms 重试兜 transcript 未刷完的竞态；缓存超 512KB 先自动归档

### 5.2 归档 —— 会话结束 / 新会话开始 / 显式触发 / 512KB 阈值

1. 无缓存 → 退出（正常收尾的会话绝不重复归档）
2. 回填：只对**属主 transcript**、且**水位线之后**的消息做补充
3. 组装正文，计算正文哈希
4. **写盘前判重**：已有存档哈希相同 → 不写第二份，只推进水位线、删缓存、退出
5. 否则写盘（首行哈希 + 正文）、推进水位线、删缓存、从磁盘全量重建索引

### 5.3 读取 —— 开场加载

先跑 `archive-pending.ps1`，再跑 `load-last-chat.ps1`：

1. 取索引 `last5`；不足 5 份按最新文件补齐
2. 按内容哈希去重；跳过 < 300 字符的空档
3. 超过 20KB 的存档截断（前 15KB + 后 5KB）
4. 用配置的 marker 输出，模型看到的就是注入的记忆

**响应对接规则：** 读取顺序是正序，但**响应对接是倒序**——最新那份存档的最后一句是开场锚点，就当它刚发生一样接话，别翻到最旧那份去开场。

### 5.4 状态 / 深度回忆 —— 用户说"不忘/remember"

同 5.3 但显式触发。别汇报进度，读完了自然回应。

### 5.5 健康自检

运行 `check-memory.ps1`：检查哈希头是否齐全、有无重复存档、有无空档、索引与磁盘是否一致、`_state.json` 水位线是否正常。

---

## 6. 平台集成

| 事件 | 钩子 | 作用 |
|------|------|------|
| SessionStart | `archive-pending.ps1` | 归档上次未完成的会话 |
| SessionStart | `load-last-chat.ps1` | 自动加载最近 5 份聊天记录 |
| Stop | `auto-save.ps1` | 每轮对话自动写入 `_pending.md` |
| SessionEnd | `archive-pending.ps1` | 会话结束归档 |

完整钩子配置见 `settings.example.json`。

---

## 7. 常见错误（v2 已从根上处理）

| 错误 | 为何错 | v2 的修法 |
|------|--------|----------|
| 重复归档同一内容（孪生） | 归档层无去重 | 水位线拦源头 + 写盘前哈希判重，双闸 |
| 索引残缺/过期 | 增量维护会陈旧 | 每次归档后从磁盘全量重建 |
| 开场加载进重复 | 按 mtime 兜底会撞 | 加载按内容哈希去重 |
| 归档空缓存 | 产生空文件 | 无 `[msg:]` 块不写盘 |
| 读到旧聊天还汇报"我读了你的记录" | 破坏沉浸感 | 别出声，直接回应 |

---

## 8. 自检

读旧聊天前：
> "我到底需不需要这个上下文，还是只是在求全？"

归档前：
> "用户一年后回看，能不能拿到一字不差的对话？"

说"我读了你的记录"之前：
> STOP。别说，直接回应。
